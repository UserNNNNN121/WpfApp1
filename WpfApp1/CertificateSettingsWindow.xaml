<Window x:Class="WpfApp1.CertificateSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки сертификата" Height="450" Width="600" Icon="/logo2.png">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox Grid.Row="0" Name="chkEnableCertificate" Content="Выдавать сертификат по завершении курса" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Шаблон сертификата" Margin="0,0,0,10" IsEnabled="{Binding IsChecked, ElementName=chkEnableCertificate}">
            <StackPanel>
                <Button Content="Выбрать шаблон..." Click="BtnSelectTemplate_Click" Margin="0,0,0,5"/>
                <TextBlock Name="txtTemplatePath" TextWrapping="Wrap"/>
                <Image Name="imgTemplatePreview" MaxHeight="150" Margin="0,5" Stretch="Uniform"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Печать и подписи" Margin="0,0,0,10" IsEnabled="{Binding IsChecked, ElementName=chkEnableCertificate}">
            <StackPanel>
                <Button Content="Добавить печать..." Click="BtnAddSeal_Click" Margin="0,0,0,5"/>
                <Image Name="imgSeal" MaxHeight="50" Margin="0,5" Stretch="Uniform"/>

                <Button Content="Добавить подпись..." Click="BtnAddSignature_Click" Margin="0,5,0,0"/>
                <Image Name="imgSignature" MaxHeight="50" Margin="0,5" Stretch="Uniform"/>
            </StackPanel>
        </GroupBox>


        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="80" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button Content="Сохранить" Width="80" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>